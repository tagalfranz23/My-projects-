SECRET_KEY=replace-with-a-long-random-production-secret
DATA_DIR=instance
SQLITE_DB_PATH=instance/minante.db
SESSION_COOKIE_SECURE=0
FLASK_DEBUG=0

# Render production values:
# DATA_DIR=/var/data
# SQLITE_DB_PATH=/var/data/minante.db
# SESSION_COOKIE_SECURE=1
